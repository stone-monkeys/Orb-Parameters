description: >
  This command echos "Hello World" using file inclusion.
# What will this command do?
# Descriptions should be short, simple, and clear.
parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  # - run:
  #     environment:
  #       PARAM_TO: <<parameters.to>>
  #     name: Hello Greeting
  #     command: <<include(scripts/greet.sh)>>
  - run:
      name: Hello World
      command: <<include(scripts/greet.sh)>>
  - run:
      name: Hello World
      command: echo $HELLO_WORLD && echo $HOWAYANOW && echo ${HOWAYANOW} && echo HOWAYANOW
  - run:
      name: Hello now
      command: echo ${HOWAYANOW} && echo HOWAYANOW
